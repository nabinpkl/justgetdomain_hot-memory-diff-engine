SNAPSHOT_PATH=/data/snapshot.bin
CORS_ORIGIN=http://localhost:3000
RUST_LOG=info
# Wordlist mode for the candidate dictionary:
#   selective (default) — curated subset, brand- and scam-bait-filtered.
#   full                — unfiltered words_full.txt (development only).
JGD_WORDLIST=selective
